backend:
  name: github
  repo: semicolon2/netlify-gatsby

media_folder: static/assets
public_folder: assets

collections:
  - name: blog
    label: Blog
    folder: blog
    create: true
    fields:
      - { name: path, label: Path }
      - { name: date, label: Ddate, widget: date }
      - { name: title, label: Title }
      - { name: body, label: Body, widget: markdown }
